source "https://rubygems.org"

# Use github-pages gem to match the GitHub Pages environment
gem "github-pages", group: :jekyll_plugins

# Minimal Mistakes Jekyll Theme
# Uncomment if you want to use it as a remote theme (removes the need for _layouts, _includes etc. in your repo)
# gem "jekyll-remote-theme"

# Additional plugins used by Minimal Mistakes
group :jekyll_plugins do
  gem "jekyll-include-cache"
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-gist"
  gem "jekyll-feed"
end
